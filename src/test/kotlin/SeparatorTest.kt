import io.kotest.core.spec.style.DescribeSpec

class SeparatorTest : DescribeSpec({
    context("구분자로 쉼표를 가질 때") {
        it("구분자를 기준으로 분리한 각 숫자의 합을 반환한다.") {
            Separator()
        }
    }


})
